{
  "devDependencies": {
    "ganache": "^7.9.2",
    "truffle": "^5.11.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "scripts": {
    "serve": "ganache",
    "build": "truffle build",
    "test": "truffle test",
    "migrate": "truffle migrate"
  }
}
